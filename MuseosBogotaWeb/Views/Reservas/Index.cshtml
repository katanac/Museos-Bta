@model IEnumerable<MuseosBogotaWeb.Contexto.Reservas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Factura.IdFactura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoEvento.NombreTipoEvento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.NombreUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaReserva)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numeroBoletas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Factura.IdFactura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoEvento.NombreTipoEvento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.NombreUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaReserva)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numeroBoletas)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdReserva }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdReserva }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdReserva })
        </td>
    </tr>
}

</table>
