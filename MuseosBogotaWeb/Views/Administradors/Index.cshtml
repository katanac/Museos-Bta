@model IEnumerable<MuseosBogotaWeb.Contexto.Administrador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.NombreUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreAddministrador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.NombreUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreAddministrador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAdministrador }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAdministrador }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAdministrador })
        </td>
    </tr>
}

</table>
